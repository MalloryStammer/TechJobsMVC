<div class="container body-content">

    <h1>@ViewBag.title</h1>

    <form action="/search/results" method="post">
        <h2>Search by:</h2>
        <p>
            @foreach (var column in ViewBag.columns)
            {
                <span>
                    <input type="radio" name="searchType" id="@column.Key" value="@column.Key" checked="@column.Key == 'all'" />
                    <label for="@column.Key">@column.Value</label>
                    @*TODO 5: possible to make first radial button default???*@

                </span>
            }
        </p>
        <p>
            <label for="searchTerm">Keyword:</label>
            <input type="text" name="searchTerm" id="searchTerm" autocomplete="off" />
        </p>
        <input type="submit" value="Search" />
    </form>
    <hr />


    @*TODO:  #4: Loop over the search results to display all job fields.
        ALSO - sending a post request to search/results but there's no action method to handle that incoming
             post request'*@


    <h1>@ViewBag.title</h1>
    <style>
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
    </style>
    @*@if (ViewBag.jobs != null)
    {*@
        @foreach (var job in ViewBag.jobs)
        {
            <table class="job-listing">
                <tr>
                    <td>ID: @job.Id</td>
                </tr>
                <tr>
                    <td>Name: @job.Name</td>
                </tr>
                <tr>
                    <td>Employer: @job.Employer</td>

                </tr>
                <tr>
                    <td>Location: @job.Location</td>
                </tr>
                <tr>
                    <td>Position Type: @job.PositionType</td>
                </tr>
                <tr>
                    <td>Skill: @job.CoreCompetency</td>
                </tr>
            </table>
            <br />
            <br />
        }
    @*}*@
</div>